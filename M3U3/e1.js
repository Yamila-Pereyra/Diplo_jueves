function promedioHistoria(nota1, nota2) {
    let promedio = (nota1 + nota2) / 2;
    return promedio;
}

// Ejemplo de uso:
let resultado = promedioHistoria(6, 8);
console.log(`El promedio de Historia es: ${resultado}`);
